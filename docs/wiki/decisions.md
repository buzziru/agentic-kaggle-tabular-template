# Decision Log (ADR-lite)

> 형식: `## [번호] 제목 — 날짜` / **결정** / **이유** / **대안·트레이드오프**. 새 결정은 위에 추가.
> 구조적/전략적 결정만 기록(왜 그렇게 정했는지). 일상 작업은 이슈 트래커.

<!-- 예시 (지우고 실제 결정으로 교체):

## [001] 베이스라인 = LightGBM CPU — {{날짜}}
- **결정**: 첫 파이프라인을 LightGBM(CPU)로 닫는다 (로드→피처→CV→OOF→제출).
- **이유**: 빠른 반복으로 검증 루프·로깅·제출 경로를 먼저 확정. 모델 다양성/튜닝은 이후.
- **대안·트레이드오프**: XGB/CatBoost 는 GPU 필요 → 다양성 단계로 미룸.

-->
