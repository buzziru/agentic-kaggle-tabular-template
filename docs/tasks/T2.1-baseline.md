# T2.1 — LGBM 베이스라인 풀 파이프라인

> `TASK.md` M2 의 세부계획 (포인터 패턴 예시).

- **마일스톤**: M2
- **상태**: 대기
- **병렬 가능**: 아니오 — 의존: M1(CV 확정) 완료 후

## 목표 (한 줄)
LightGBM 으로 로드→피처→CV→OOF→제출까지 **파이프라인 전체를 한 번 닫는다**(점수보다 배선 검증).

## 성공 기준 (verify)
- `uv run python -m src.train exp_id=exp_001 "notes='lgbm baseline'"` 무오류 완료
- `experiments/oof/exp_001.csv`(=[id, oof]) · `submissions/exp_001.csv` · `logs/exp_001.json` 생성
- 로그에 fold별 `best_iter` 기록 + 모두 cap 미접촉(수렴 확인)

## 접근 (단계 → verify)
1. `config.py` 컬럼·METRIC 확인 → verify: `features.get_feature_cols` 가 기대 컬럼 반환
2. baseline 실행(`features=base`, augment off) → verify: OOF 점수 출력
3. `scripts/summarize.py` 로 로그 확인 → verify: exp_001 행 등장
4. (T2.2) 제출 후 `predict.record_submission` → verify: OOF≈LB 갭 기록

## 건드리는 파일
- `src/config.py`(컬럼 확정) · `conf/model/lgbm.yaml`(objective/metric) · `conf/features/base.yaml`

## 산출물
- exp_001 (OOF·submission·log) · `docs/wiki/decisions.md` 에 "베이스라인 = LGBM" 1줄

## 결과 / 결론
- {{완료 시: OOF=___, LB=___, 갭=___}}
