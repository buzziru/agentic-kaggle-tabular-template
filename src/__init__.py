"""프로젝트 소스 패키지.

⚠️ 이 파일을 비우지 말 것 — Kaggle 등에서 `sys.path` 에 추가했을 때 빈 `__init__.py` 는
`src` 가 namespace 패키지로 shadowing 돼 `from src import config` 가 깨질 수 있다
(docs/wiki/kaggle_jobs.md 교훈 3). 최소 1줄(이 docstring)을 유지한다.
"""
