# 시크릿 탐지 정규식 (1줄 1패턴, # 주석). guard_bash.py 가 git add/commit 명령에
# 대해 IGNORECASE 로 매칭한다. 매칭되면 T0 하드 차단(우회 불가).
#
# ⚠️ 오차단이 나면 훅을 끄지 말고 여기서 패턴을 조정하라
#    (CLAUDE.md "외부 인프라 가드: 1회 발생 시 즉시 코드화" 원칙).
# ⚠️ 이 파일은 frozen/gitignore 대상이 아니다 — 그러면 갱신이 막혀 순환에 빠진다.

(?:^|[\s=/'"])[^\s'";|&]*\.(?:env|key|pem|pfx|p12)\b
(?:^|[\s=/'"/])(?:[^\s'";|&]*[/.])?(?:credentials?|secrets?|service[_-]?account)\.(?:json|ya?ml|ini|cfg|conf|txt|env|pem|key)\b
kaggle\.json
id_rsa
