# 줄바꿈 일관성 — 저장소 표준 = LF (Windows 체크아웃 시 CRLF 경고 방지)
* text=auto eol=lf

# 셸 스크립트는 항상 LF (CRLF 면 실행 깨짐)
*.sh text eol=lf

# 바이너리 — 변환 금지
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.zip    binary
*.gz     binary
*.pkl    binary
*.joblib binary
*.ckpt   binary
